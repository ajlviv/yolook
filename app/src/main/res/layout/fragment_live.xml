<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:implementationMode="compatible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:scaleType="fillCenter" />

    <!-- Filtered view-mode frame renderer. Hidden (GONE) in NORMAL view mode.
         Opaque black background so the raw preview underneath never shows through
         before the first filtered frame is baked. -->
    <ImageView
        android:id="@+id/imageFilterPreview"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#000000"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.yolo.detector.ui.overlay.BoundingBoxOverlay
        android:id="@+id/overlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Performance Stats Bar -->
    <TextView
        android:id="@+id/tvStats"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="48dp"
        android:layout_marginBottom="16dp"
        android:background="#80000000"
        android:padding="8dp"
        android:text="FPS: 0.0  Latency: 0ms  Objects: 0"
        android:textColor="#00E676"
        android:textSize="14sp"
        android:typeface="monospace"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvModelError"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:background="#D32F2F"
        android:padding="12dp"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvStats" />

    <!-- Monitoring quick toggle above the snapshot FAB. Same style as the count
         toggle; green fill = monitoring active (keeps screen on + dims brightness). -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/fabMonitoring"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="16dp"
        android:contentDescription="@string/fab_toggle_monitoring"
        android:insetLeft="0dp"
        android:insetTop="0dp"
        android:insetRight="0dp"
        android:insetBottom="0dp"
        android:text="M"
        android:textSize="22sp"
        app:backgroundTint="#212121"
        app:cornerRadius="28dp"
        app:layout_constraintBottom_toTopOf="@id/fabModeToggle"
        app:layout_constraintEnd_toEndOf="parent"
        app:strokeColor="#00E676"
        app:strokeWidth="2dp"
        android:textColor="#00E676" />

    <!-- Count toggle: 'D' = detect labels, 'C' = count numbers inside boxes.
         Toggles object counting only; the view mode is untouched. Docked above the snapshot FAB. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/fabModeToggle"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="16dp"
        android:contentDescription="@string/fab_toggle_mode"
        android:insetLeft="0dp"
        android:insetTop="0dp"
        android:insetRight="0dp"
        android:insetBottom="0dp"
        android:text="D"
        android:textSize="22sp"
        app:backgroundTint="#212121"
        app:cornerRadius="28dp"
        app:layout_constraintBottom_toTopOf="@id/fabSnapshot"
        app:layout_constraintEnd_toEndOf="parent"
        app:strokeColor="#00E676"
        app:strokeWidth="2dp"
        android:textColor="#00E676" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabSnapshot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        android:contentDescription="@string/fab_snapshot"
        android:src="@android:drawable/ic_menu_camera"
        app:backgroundTint="#00E676"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="#000000" />

</androidx.constraintlayout.widget.ConstraintLayout>
